<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.dlrtn.websocket.chat.mapper.UserMapper">

    <insert id="saveUser" parameterType="User">
        INSERT INTO TB_USER (
            USER_ID,
            PASSWORD,
            REAL_NAME,
            AUTH_ROLE,
            CREATED_AT,
            UPDATED_AT
        ) VALUES (
            #{userId},
            #{password},
            #{realName},
            #{authRole},
            #{createdAt},
            #{updatedAt}
        )
    </insert>

    <delete id="removeUser" parameterType="String">
        DELETE
        FROM
            TB_USER
        WHERE
            USER_ID = #{userId}
    </delete>

    <select id="findByUserId" parameterType="String" resultType="User">
        SELECT
            USER_NO,
            USER_ID,
            PASSWORD,
            REAL_NAME,
            AUTH_ROLE,
            CREATED_AT,
            UPDATED_AT
        FROM
            TB_USER
        WHERE
            USER_ID = #{userId}
    </select>

    <select id="login" parameterType="String" resultType="User">
        SELECT
            USER_NO,
            USER_ID,
            PASSWORD,
            REAL_NAME,
            AUTH_ROLE,
            CREATED_AT,
            UPDATED_AT
        FROM
            TB_USER
        WHERE
            USER_ID = #{userId}
            AND
            PASSWORD = #{password}
    </select>

</mapper>
